1 - create a repo
2 - mkdir && cd newdir
3 - git clone repo
4 - pip install django
5 - pip install --upgrade pip
6 - pip install djangorestframework
7 - pip freeze -> requirements.txt
8 - django-admin startproject restb .
9 - django-admin startapp tickets
10 - in settings.py -> installed apps -> 'rest_framework',

----------------------------------------------------------
ticket app:
===========
settings.py -> register 'tickets' under INSTALLED_APPS


Postgres Database Config
========================
1 - sudo systemctl start/status postgresql
2 - create Database

3 - in settings -> DATABASE
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.postgresql',
            'NAME': 'mydb',          # database name from pgAdmin
            'USER': 'postgres',      # your PostgreSQL username
            'PASSWORD': 'yourpassword',  # your PostgreSQL password
            'HOST': 'localhost',     # or the server IP if remote
            'PORT': '5432',          # default PostgreSQL port
        }
    }

4 - pip install psycopg2-binary

#configure admin
----------------
python manage.py createsuperuser
username : admin
email    : admin@gmail.com
password : SuperCoder

5 - python manage.py makemigrations
6 - python manage.py migrate

--------------------------------------------------------------------------------------------

#Working with REST Serializers:
===============================
1 - under tickets app -> create : serializers.py
(works as a medium between views & models - a translator to json)
2 - in it -> 
    from rest_framework import serializers
    from tickets.models import *

--------------------------------------------------------------------------------------------

#Working with Authentication
============================
1 - Global Authentication (using username & password)
    settings.py -> REST_FRAMEWORK = {
        'DEFAULT_AUTHENTICATION_CLASSES' :
        ['rest_framework.authentication.TokenAuthentication'],
        'DEFAULT_PERMISSION_CLASSES' :
        ['rest_framework.permissions.IsAuthenticated'],
    }

2 - for Specific view method auth:

    from rest_framework.authentication import BaseAuthentication, TokenAuthentication
    from rest_framework.permissions import IsAuthenticated

    #inside method
    authentication_classes  = [TokenAuthentication]
    permission_classes      = [IsAuthenticated]


3 - auth token
    in settings.py -> INSTALLED_APPS -> 'rest_framework.authtoken',

    REST_FRAMEWORK = {

    #Allow Any // IsAuthenticated // IsAdminUser // IsAuthenticatedReadonly

    'DEFAULT_AUTHENTICATION_CLASSES' :
    ['rest_framework.authentication.BasicAuthentication'],
    # 'DEFAULT_PERMISSION_CLASSES' :
    # ['rest_framework.permissions.IsAuthenticated'],

    }

    - in urls.py
    from rest_framework.authtoken.views import obtain_auth_token

    # 11 - Token Authentication
    path('api_token_auth', obtain_auth_token),

    - Migrate

* to automate token generation :
    - create a 'signal' on every new user create a token!
    goto models.py
        from django.db.models.signals import post_save
        from django.dispatch import receiver
        from rest_framework.authtoken.models import Token
        from django.conf import settings 

        @receiver(post_save, sender=settings.AUTH_USER_MODEL)
        def user_create_token_create(sender, instance, created, **kwargs):
            if created:
                Token.objects.create(user=instance)


    